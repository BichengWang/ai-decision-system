# Development log

This is a narrative engineering record, not actor-level or version-by-version Git provenance. The candidate files first entered a larger working repository together in a bulk commit. Before attributing authorship, identify who designed, wrote, reviewed, changed, and ran each material component and corroborate that attribution independently of this log.

| Date | Event |
|---|---|
| 2026-09-19 | Package candidate 0.1.5 local monorepo integration: retained an independent project environment and specification 0.1.3; added source/export identity instructions, a field-level comparison helper, environment records, and a technical report. The local full suite passed 35 tests. Two explicit fresh runs produced byte-identical result and manifest artifacts; result SHA-256 `59dfe7ba224d220d26e5d97871078ffa2f8b9806e6446c684783cd96fabf6eb6`. Compared with the retained 0.1.4 result, only the package-version field changed. Thread environment limits were set to 1 and preexisting package caches were used. These are local development checks, not an attested clean-machine run, unaffiliated reproduction, rights clearance, maintainer acceptance, or public release. |
| 2026-09-17 | Run 0: 60,000 sessions, near-linear ground truth. Held-out partitions were small (about 2,300 rows) because time and merchant separation are intersected. Gates **BLOCKED** release: the candidate lost to the logistic baseline on log loss and Brier, and worst-slice ECE was 0.066. A historical summary, not a reproducible retained run, is preserved at `docs/dev-runs/run-0-BLOCK-RESULTS.md`. |
| 2026-09-17 | Generator enlarged to 300,000 sessions and given documented non-linear structure. Performance thresholds were not changed. Run 1: all nine 0.1.0 candidate gates appeared to pass on the worst of five seeds; the guarded update was rejected. Review then found that the routing gate used a 1.0 fallback with zero observed critical cases, while the update cycle used only seed 11 and a 200-row rather than specified 500-row slice minimum. Candidate 0.1.0 was not published or tagged. |
| 2026-09-17 | Candidate 0.1.1 correctness repair: added a 16-case review fixture with 12 required-review cases and a minimum-denominator condition; applied the guarded offer-model cycle to all five seeds, the 500-row slice minimum, and eight applicable gates with ranking alone excluded; added missing negative gate tests; normalized the simulated reference field to `current_reference`; made comparative gate labels and metadata identify the actual comparator while retaining the unevaluated ranking gate's baseline identity; aligned the stability-gate label with the near-equivalent-input terminology; qualified smoke/canary steps as deployment-specific because no canary protocol is implemented here; and narrowed byte-identical reproduction to the deterministic result and manifest artifacts. Regenerated results: the primary candidate passes all nine gates, while the update is rejected because the per-gate worst-of-five aggregation fails log-loss delta and worst-slice ECE on different seeds. Maintainer review remains pending; no public repository, annotated tag, or release exists. |
| 2026-09-17 | Candidate 0.1.2 correctness repair: review found that 0.1.1 derived the fixture's expected-required cases with the same predicate used by the router and did not score the four nonrequired controls. Review also found that the constraint counter mapped arbitrary outputs to the nearest permitted discount. Candidate 0.1.2 stores fixture expectations independently, requires 12/12 required cases and 0/4 nonrequired controls to route, rejects non-enumerated discounts, and adds negative tests for both defects. MRR was added to the human summary, and noncanonical reproduction now requires per-metric tolerances plus invariant decisions and gate dispositions. Regenerated results preserve the nine-gate primary PASS and the adverse guarded-update rejection on two of eight applicable gates. Candidate 0.1.1 was never published or tagged. Maintainer review remains pending; no public repository, annotated tag, or release exists. |
| 2026-09-17 | Candidate 0.1.3 correctness repair: review found that 0.1.2 tested a router helper while the live decision path called the underlying predicate directly; worst-slice ECE returned 0 when no slice met the minimum; and the input guard counted only three named fields rather than every non-authorized model/policy read. Candidate 0.1.3 wires the live path and fixture to the same runtime-resolved router, blocks the existing worst-slice gate when zero slices are eligible, adopts an authorized-feature allowlist, adds negative tests, and narrows exact stored-hash equality to the attested producing environment. Candidate 0.1.2 was never published or tagged. Maintainer review remains pending; no public repository, annotated tag, or release exists. |
| 2026-09-18 | Package candidate 0.1.4 release-control repair: review found that a primary gate decision of `BLOCK` still exited with process status 0 and that a run could reuse a nonempty output directory, risking stale mixed artifacts. The runner now returns status 1 for a primary `BLOCK`, refuses a file or nonempty output location before execution, and permits a new or existing empty directory. Regression coverage raises the suite to 12 tests. The generator, partitions, metrics, thresholds, gates, models, and specification remain at 0.1.3; generated artifacts were regenerated under package candidate 0.1.4. Candidate 0.1.3 was never published or tagged. Maintainer review remains pending; no public repository, annotated tag, or release exists. |
